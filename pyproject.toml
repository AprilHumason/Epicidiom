[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "epicidiom"
version = "0.1.0"
description = "A simple Python starter project."
readme = "README.md"
requires-python = ">=3.11"

[project.scripts]
epicidiom = "epicidiom.__main__:main"
